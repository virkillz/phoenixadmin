
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="A fully featured admin theme which can be used to build CRM, CMS, etc.">
        <meta name="author" content="Coderthemes">

        <link rel="shortcut icon" href="images/favicon_1.ico">

        <title>Get Top Up Engine</title>

        <!-- Base Css Files -->
        <link href="<%= static_path(@conn, "/css/bootstrap.min.css") %>" rel="stylesheet" />

        <!-- Font Icons -->
        <link href="<%= static_path(@conn, "/images/font-awesome/css/font-awesome.min.css") %>"  rel="stylesheet" />
        <link href="<%= static_path(@conn, "/images/ionicon/css/ionicons.min.css") %>"  rel="stylesheet" />
        <link href="<%= static_path(@conn, "/css/material-design-iconic-font.min.css") %>"  rel="stylesheet">

        <!-- animate css -->
        <link href="<%= static_path(@conn, "/css/animate.css") %>"  rel="stylesheet" />

        <!-- Waves-effect -->
        <link href="<%= static_path(@conn, "/css/waves-effect.css") %>"  rel="stylesheet">

        <!-- Custom Files -->
        <link href="<%= static_path(@conn, "/css/helper.css") %>"  rel="stylesheet" type="text/css" />
        <link href="<%= static_path(@conn, "/css/style.css") %>"  rel="stylesheet" type="text/css" />

        <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
        <![endif]-->
        <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
        <script src="<%= static_path(@conn, "/js/modernizr.min.js") %>"></script>
        
    </head>
    <body>

                    <%= render @view_module, @view_template, assigns %>

        
    	<script>
            var resizefunc = [];
        </script>
        <!-- jQuery  -->
        <script src="<%= static_path(@conn, "/js/jquery.min.js") %>"></script>
        <script src="<%= static_path(@conn, "/js/bootstrap.min.js") %>"></script>
        <script src="<%= static_path(@conn, "/js/waves.js") %>"></script>
        <script src="<%= static_path(@conn, "/js/wow.min.js") %>"></script>
        <script src="<%= static_path(@conn, "/js/jquery.nicescroll.js") %>" type="text/javascript"></script>
        <script src="<%= static_path(@conn, "/js/jquery.scrollTo.min.js") %>"></script>
        <script src="<%= static_path(@conn, "/images/jquery-detectmobile/detect.js") %>"></script>
        <script src="<%= static_path(@conn, "/images/fastclick/fastclick.js") %>"></script>
        <script src="<%= static_path(@conn, "/images/jquery-slimscroll/jquery.slimscroll.js") %>"></script>
        <script src="<%= static_path(@conn, "/images/jquery-blockui/jquery.blockUI.js") %>"></script>


        <!-- CUSTOM JS -->
        <script src="<%= static_path(@conn, "/js/jquery.app.js") %>"></script>
        <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
	
	</body>
</html>